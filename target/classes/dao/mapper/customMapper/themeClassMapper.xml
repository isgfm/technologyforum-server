<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guo.technologyforum.dao.mapper.customMapper.CustomThemeClassMapper">

    <resultMap type="com.guo.technologyforum.dao.entity.ThemeClass" id="themeClassResultMap">
        <!-- id表示查询结果集中唯一标识 column:查询出的列名
        property:type所指定的POJO中的属性名
        最终reslutMap对column和property做一个映射关系(对应关系)
        -->

    </resultMap>

    <select id="getClass" resultType="com.guo.technologyforum.dao.entity.ThemeClass">
        select * from t_theme_class where c_type=#{type} and n_depth=#{depth}
    </select>

    <select id="getClassesByparentId" resultType="com.guo.technologyforum.dao.entity.ThemeClass">
        select * from t_theme_class where n_parentId=#{parentId}
    </select>

</mapper>